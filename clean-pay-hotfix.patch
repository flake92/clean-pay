diff --git a/docs/production-hardening-and-branding-task.md b/docs/production-hardening-and-branding-task.md
index 4ee6687..fb3c0f0 100644
--- a/docs/production-hardening-and-branding-task.md
+++ b/docs/production-hardening-and-branding-task.md
@@ -205,7 +205,7 @@ Scope:
 
 - Production app падает с понятной ошибкой при неверной required configuration.
 - Валидируются связанные настройки:
-  - `TURNSTILE_ENABLED=true` требует `NEXT_PUBLIC_TURNSTILE_SITE_KEY` и `TURNSTILE_SECRET_KEY`;
+  - `TURNSTILE_ENABLED=true` требует `TURNSTILE_SITE_KEY` и `TURNSTILE_SECRET_KEY`;
   - `COOKIE_SAMESITE=none` требует `COOKIE_SECURE=true`;
   - так как subscription URL должен приходить из Remnawave, production требует одновременно `REMNAWAVE_API_BASE_URL` и `REMNAWAVE_TOKEN`;
   - Telegram settings должны быть внутренне согласованы;
diff --git a/src/app/globals.css b/src/app/globals.css
index bb6319a..2b26b67 100755
--- a/src/app/globals.css
+++ b/src/app/globals.css
@@ -241,13 +241,14 @@ body {
   box-shadow: 0 1px 3px rgba(15, 23, 42, 0.12);
   color: #334155;
   min-width: 0;
+  overflow: hidden;
 }
 
 .p-card .p-card-body {
   display: flex;
   flex-direction: column;
-  gap: 1rem;
-  padding: 1.5rem;
+  gap: 1.125rem;
+  padding: 2rem !important;
 }
 
 .p-card .p-card-title {
@@ -255,8 +256,9 @@ body {
   font-size: 1.5rem;
   font-weight: 700;
   line-height: 1.25;
-  margin: 0;
+  margin: 0 !important;
   overflow-wrap: anywhere;
+  padding-top: 0.125rem;
 }
 
 .p-card .p-card-subtitle {
@@ -268,11 +270,11 @@ body {
 
 .p-card .p-card-content {
   min-width: 0;
-  padding: 0;
+  padding: 0 !important;
 }
 
 .p-card .p-card-footer {
-  padding: 0;
+  padding: 0 !important;
 }
 
 .auth-account-summary {
diff --git a/src/frontend/components/auth-forms.tsx b/src/frontend/components/auth-forms.tsx
index abb32a3..cd2e2c3 100755
--- a/src/frontend/components/auth-forms.tsx
+++ b/src/frontend/components/auth-forms.tsx
@@ -1,6 +1,6 @@
 "use client";
 
-import { createContext, useContext, useEffect, useMemo, useState, type FormEvent, type ReactNode } from "react";
+import { createContext, useContext, useMemo, useState, type FormEvent, type ReactNode } from "react";
 
 import { Button } from "primereact/button";
 import { InputText } from "primereact/inputtext";
@@ -503,10 +503,6 @@ export function TelegramLoginButton({ redirectTo = "/cabinet" }: { redirectTo?:
   const turnstile = useAuthTurnstile();
   const missingTurnstileMessage = missingTurnstileTokenMessage(turnstile.siteKey);
 
-  useEffect(() => {
-    setState((current) => (current.error === missingTurnstileMessage ? { loading: false, error: null } : current));
-  }, [missingTurnstileMessage, turnstile.token]);
-
   async function onClick() {
     if (turnstile.enabled && !turnstile.token) {
       setState({ loading: false, error: missingTurnstileMessage });
diff --git a/start.sh b/start.sh
index d951938..606a5e6 100644
--- a/start.sh
+++ b/start.sh
@@ -206,8 +206,7 @@ validate_env() {
   esac
 
   if [ "$(env_value TURNSTILE_ENABLED false)" = "true" ]; then
-    site_key=$(env_value NEXT_PUBLIC_TURNSTILE_SITE_KEY "$(env_value TURNSTILE_SITE_KEY)")
-    [ -n "$site_key" ] || fail "NEXT_PUBLIC_TURNSTILE_SITE_KEY is required when TURNSTILE_ENABLED=true"
+    required_env TURNSTILE_SITE_KEY
     required_env TURNSTILE_SECRET_KEY
   fi
 
