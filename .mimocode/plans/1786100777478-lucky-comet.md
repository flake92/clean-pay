# Рефакторинг auth/passkeys.ts

## Контекст
Продолжаем Фазу 4 рефакторинга. Файлы `idempotency.ts`, `reconciliation.ts`, `email-verification.ts` завершены. `history-sync.ts` и `records.ts` пропущены (транзакции/raw SQL). Следующий — `auth/passkeys.ts`.

## Анализ

### Транзакционные вызовы (1 блок) — оставляем как есть
| Функция | Строка | Причина |
|---------|--------|---------|
| `deleteOwnedPasskey` | 493 | `SELECT FOR UPDATE` + count-then-delete |

### Простые вызовы — можно рефакторить (1 штука)
| Функция | Строка | Вызов | Действие |
|---------|--------|-------|----------|
| `finishPasskeyRegistration` | 289 | `prisma.webUser.update` | → `userStore.update` |

### Простые вызовы — НЕ рефакторим (11 штук)
| Функция | Строка | Вызов | Причина отказа |
|---------|--------|-------|----------------|
| `consumeChallenge` | 60 | `prisma.webAuthnChallenge.findFirst` | Нет `WebAuthnChallengeStore` |
| `beginPasskeyRegistration` | 198 | `prisma.webAuthnChallenge.create` | Нет `WebAuthnChallengeStore` |
| `finishPasskeyRegistration` | 254-282 | `prisma.webAuthnCredential.*` (3 вызова) | Нет `WebAuthnCredentialStore`, tightly coupled retry |
| `beginPasskeyLogin` | 328 | `prisma.webUser.findUnique` (+ relation) | `UserStore` не возвращает `webAuthnCredentials` |
| `beginPasskeyLogin` | 352 | `prisma.webAuthnChallenge.create` | Нет `WebAuthnChallengeStore` |
| `recordPasskeyUse` | 381, 388 | `prisma.webAuthnCredential.*` | Нет `WebAuthnCredentialStore` |
| `finishPasskeyLogin` | 414 | `prisma.webAuthnCredential.findUnique` | Нет `WebAuthnCredentialStore` |
| `listPasskeys` | 474 | `prisma.webAuthnCredential.findMany` | Нет `WebAuthnCredentialStore` |

## План реализации

### Шаг 1: Заменить 1 вызов в `passkeys.ts`
- Импортировать `getServiceRegistry`
- Заменить `prisma.webUser.update` на строке 289 на `userStore.update`

### Шаг 2: Оставить остальные вызовы как есть
Создание `WebAuthnChallengeStore` и `WebAuthnCredentialStore` — отдельная задача, не входит в текущий рефакторинг.

## Файлы для изменения
- `src/backend/auth/passkeys.ts` (замена 1 вызова)

## Верификация
1. `npm run typecheck` — без ошибок
2. `npm run lint` — 0 ошибок
3. `npx vitest run` — все 549 тестов зелёные
4. CI на GitHub — зелёный
