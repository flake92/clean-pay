# Рефакторинг payments/reconciliation.ts

## Контекст
Продолжаем Фазу 4 рефакторинга. Файл `payments/idempotency.ts` завершён. Следующий — `payments/reconciliation.ts`.

## Анализ

### Простые вызовы (2 штуки) — можно рефакторить
| Функция | Строка | Вызов | Действие |
|---------|--------|-------|----------|
| `settlePaymentReconciliation` | 510 | `prisma.paymentOperation.findUnique` (select `reconcileFailureCount`) | → `store.findReconcileFailureCount` |
| `failPaymentReconciliation` | 533 | `prisma.paymentOperation.findUnique` (select `reconcileFailureCount`) | → `store.findReconcileFailureCount` |

### Транзакционные вызовы (4 блока) — оставляем как есть
| Функция | Строка | Причина |
|---------|--------|---------|
| `claimUnknownPaymentOperation` | 95 | CTE с `FOR UPDATE SKIP LOCKED` |
| `completeReconciledPayment` | 217 | Multi-table atomic ops + `lockPaymentUpstreamOwner` |
| `releaseReconciliationClaim` | 328 | `FOR KEY SHARE` + atomic audit write |
| `resetMissingUpstreamOperation` | 438 | `lockPaymentUpstreamOwner` + optimistic lock |

## План реализации

### Шаг 1: Расширить `PaymentOperationStore`
Добавить метод `findReconcileFailureCount(id: string): Promise<number | null>` в интерфейс и реализацию.

### Шаг 2: Рефакторить `reconciliation.ts`
- Импортировать `getServiceRegistry`
- Заменить 2 простых вызова на `paymentOperationStore.findReconcileFailureCount`
- Оставить 4 транзакционных блока как есть

## Файлы для изменения
- `src/backend/services/payment-operation-store.ts` (добавить метод)
- `src/backend/services/prisma-payment-operation-store.ts` (добавить реализацию)
- `src/backend/payments/reconciliation.ts` (рефакторинг 2 вызовов)

## Верификация
1. `npm run typecheck` — без ошибок
2. `npm run lint` — 0 ошибок
3. `npx vitest run` — все 549 тестов зелёные
4. CI на GitHub — зелёный
