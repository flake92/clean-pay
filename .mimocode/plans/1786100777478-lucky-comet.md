# Рефакторинг payments/idempotency.ts

## Контекст
Продолжаем Фазу 4 рефакторинга архитектуры. Следующий файл по приоритету — `payments/idempotency.ts` (критичный путь оплаты).

## Анализ

### Простые вызовы (7 штук) — можно рефакторить
| Функция | Строка | Вызов | Действие |
|---------|--------|-------|----------|
| `findOperation` | 279 | `prisma.paymentOperation.findUnique` | → `store.findByUserAndKey` |
| `beginPaymentOperation` | 535 | `prisma.paymentOperation.updateMany` | → `store.updateMany` |
| `bindPaymentOperationUpstreamOwner` | 664 | `prisma.paymentOperation.updateMany` | → `store.updateMany` |
| `bindPaymentOperationUpstreamOwner` | 680 | `prisma.paymentOperation.findUnique` | → `store.findById` |
| `markPaymentOperationDispatched` | 721 | `prisma.paymentOperation.updateMany` | → `store.updateMany` |
| `settlePaymentOperationBeforeDispatchFailure` | 937 | `prisma.paymentOperation.updateMany` | → `store.updateMany` |
| `settlePaymentOperationAfterDispatchFailure` | 1032 | `prisma.paymentOperation.updateMany` | → `store.updateMany` |

### Транзакционные вызовы (3 блока) — оставляем как есть
| Функция | Строка | Причина |
|---------|--------|---------|
| `createOrFindOperation` | 300 | `prisma.$transaction` + `FOR UPDATE` + advisory lock |
| `beginPaymentOperation` | 571 | `prisma.$transaction` + optimistic lock |
| `completePaymentOperationSuccess` | 814 | `prisma.$transaction` + multi-table atomic ops |

## План реализации

### Шаг 1: Создать интерфейс `PaymentOperationStore`
Файл: `src/backend/services/payment-operation-store.ts`

```typescript
export interface PaymentOperationStore {
  findByUserAndKey(userId: string, idempotencyKeyHash: string): Promise<PaymentOperationRecord | null>;
  findById(id: string): Promise<Partial<PaymentOperationRecord> | null>;
  updateMany(where: UpdateWhereInput, data: UpdateDataInput): Promise<number>;
}
```

### Шаг 2: Создать реализацию `PrismaPaymentOperationStore`
Файл: `src/backend/services/prisma-payment-operation-store.ts`

### Шаг 3: Добавить в реестр сервисов
Файл: `src/backend/services/registry.ts`

### Шаг 4: Рефакторить `idempotency.ts`
- Импортировать `getServiceRegistry`
- Заменить 7 простых вызовов на методы сервиса
- Оставить 3 транзакционных блока как есть

### Шаг 5: Обновить тесты
- Замокать `PaymentOperationStore` в тестах
- Обновить ожидания на использование моков сервисов

## Файлы для изменения
- `src/backend/services/payment-operation-store.ts` (новый)
- `src/backend/services/prisma-payment-operation-store.ts` (новый)
- `src/backend/services/registry.ts` (добавить PaymentOperationStore)
- `src/backend/payments/idempotency.ts` (рефакторинг)
- `tests/unit/backend/payments/idempotency*.test.ts` (обновить моки)

## Верификация
1. `npm run typecheck` — без ошибок
2. `npm run lint` — 0 ошибок
3. `npx vitest run` — все 549 тестов зелёные
4. CI на GitHub — зелёный
