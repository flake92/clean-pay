# Project configuration

- `typescript/` contains secondary TypeScript project configurations.
- `vitest/` contains unit, integration, end-to-end and workspace test configurations.

Root-level configuration files are retained only when npm, Next.js, Prisma, PostCSS, ESLint, pnpm, editors or runtime-version managers discover them there by convention.
