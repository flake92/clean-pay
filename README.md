# Clean Pay / CleanVPN Web Cabinet

## ???????

Clean Pay ? ????????? web-??????? CleanVPN ?? Next.js App Router. Frontend ???????? ?????? ? ??????????? BFF; BFF ???????? ?????????? ? Remnashop Public API, ????????? web-cookie ????????, ????????? cache DB, audit-?????? ? rate-limit ????? Redis.

### ???????????

- `web`: Next.js frontend + BFF.
- `postgres`: ??????????? cache DB web-???????? ????? Prisma.
- `redis`: rate-limit storage. ? production ???? ??????? ? ??????????? Redis ? ??????? ? ??????? Redis.
- `caddy`: reverse proxy ?? 80/443 ? ?????????????? TLS.
- `Remnashop`: ??????? ???????? ??????-?????? ????????, ???????, ????? ? VPN-????????.
- `SMTP`: ??????? SMTP ??? ???????? ??????????? ? ??????? ???????? ??????????.
- `Cloudflare Turnstile`: ???????????? server-side ???????? ?????????????? auth-????????.

### Devcontainer

??? ??????? ?????????? ??????????? ?????? ????????????? devcontainer `clean-pay_devcontainer-app-1`. Devcontainer ????????? ???????? dev-??????? ? Docker-????:

- PostgreSQL: `db:5432`
- Redis: `redis:6379`
- Remnashop mock: `http://remnashop-mock:8080/api/v1/public`
- SMTP Mailpit: `mailpit:1025`, UI ?? `http://localhost:8025`
- Turnstile mock: `http://turnstile-mock:8787/turnstile/v0/siteverify`

?? ????????? app ???????? ??? env ?? `.devcontainer/docker-compose.yml`. ???????? Redis/Remnashop/SMTP/Turnstile ????? ?????????? ???????????????? ??????????????? env.

```bash
cd /workspaces/clean-pay
npm install
npm run prisma:generate
npm run prisma:migrate -- --name init
npm run dev
```

Mock preview:

```bash
npm run build:mock
npm run start:mock -- --hostname 0.0.0.0
```

Normal mode ? devcontainer ???? ????? ???????? ??? ???????? Remnashop, ?????? ??? `REMNASHOP_API_BASE_URL` ????????? ?? dev Remnashop mock.

### ???????? ???????

```bash
npm run lint
npm run build
npm run build:mock
npm run start:normal -- --hostname 0.0.0.0
npm run start:mock -- --hostname 0.0.0.0
npm run smoke:normal
npm run smoke:mock
```

### ?????????? ?????????

??. `.env.example` ? `.env.production.example`. ?????? ??????:

- App URLs: `APP_URL`, `NEXT_PUBLIC_APP_URL`, `APP_DOMAIN`
- DB: `DATABASE_URL`, `POSTGRES_PASSWORD`, `POSTGRES_DB`
- Redis: `REDIS_URL`
- Remnashop: `REMNASHOP_API_BASE_URL`
- Web secrets: `WEB_JWT_SECRET`, `WEB_REFRESH_SECRET`, `AUDIT_IP_HASH_SECRET`
- Cookies: `COOKIE_SECURE`, `COOKIE_SAMESITE`
- SMTP: `SMTP_HOST`, `SMTP_PORT`, `SMTP_USER`, `SMTP_PASSWORD`, `SMTP_FROM`
- Telegram OIDC: `TELEGRAM_OIDC_*`
- Turnstile: `TURNSTILE_ENABLED`, `NEXT_PUBLIC_TURNSTILE_SITE_KEY`, `TURNSTILE_SECRET_KEY`, `TURNSTILE_VERIFY_URL`
- Support: `SUPPORT_ENABLED`, `SUPPORT_EMAIL`, `SUPPORT_TELEGRAM_USERNAME`, `SUPPORT_FAQ_URL`

??????? ?? ?????????. Production ???????? ??????? ?????? ?? ??????? ??? ? secret manager.

### Production deploy: Caddy + ?????????? Redis

1. ??????????? `.env.production.example` ? `.env` ? ????????? ???????? ????????.
2. ?????????, ??? DNS `oplata.clear-vpn.org` ????????? ?? ??????.
3. ?????????:

```bash
docker compose --env-file .env -f docker-compose.prod.yml up -d --build
```

??????: `web`, `postgres`, `redis`, `caddy`. Caddy ??????? `80/443` ? ??? ?????????/????????? TLS-??????????.

### Production deploy: ??????? Redis

???? Redis ??? ??????????, ??????? `REDIS_URL` ? `.env` ? ?????????:

```bash
docker compose --env-file .env -f docker-compose.prod.external-redis.yml up -d --build
```

??????: `web`, `postgres`, `caddy`; Redis ?? ??????????? ???????????.

### Cloudflare

????????????? ?????: Cloudflare SSL/TLS `Full (strict)`. DNS-?????? `oplata.clear-vpn.org` ?????? ???????? ?? ??????. Caddy ????????? origin TLS ?????????????. ???? ???????????? Cloudflare proxy, ?????????, ??? HTTP/HTTPS challenge ???????? ???????, ???? ????????? Caddy DNS challenge ????????.

### Healthcheck

- `GET /api/health` ? ????????? ??????????? ??????.
- `GET /api/health/readiness` ? ????????? readiness-????????: DB, Redis, Remnashop `/plans/public`, SMTP TCP-connect. ??? ??????????? SMTP ??????? JSON warning ? logs.

### Audit ? ????

???????????????? ??????? ??????? ? `AuditLog`: login/register/logout, e-mail verification, payments, extension, devices, promocode, reissue, Telegram/account link. ??????????? ?????? ??????? JSON-?????? ? `console`. ? audit ?? ??????? ??????, JWT, refresh tokens, cookie, verification codes, SMTP password, Turnstile token ? raw upstream details. IP ???????? ?????? ??? hash ????? `AUDIT_IP_HASH_SECRET`.

### Acceptance smoke

```bash
npm run smoke:mock
npm run smoke:normal
```

Smoke ????????? `/api/health`, login, cookie-authenticated `/cabinet`, CSS theme ? `/api/health/readiness`. ??? real Remnashop ????????????? ??????? ????????? ???????????, e-mail confirmation, ??????, ?????????, ??????? ????????, VPN-??????, ??????????, ????????? ?????? ? ?????? upstream.

### ??????????

```bash
git pull
docker compose --env-file .env -f docker-compose.prod.yml up -d --build
docker compose --env-file .env -f docker-compose.prod.yml exec web npx prisma migrate deploy
```

??? external Redis ???????? compose-???? ?? `docker-compose.prod.external-redis.yml`.

### ?????

1. ??????? ?????????? git tag/commit.
2. ??????????? compose.
3. ????????? `/api/health` ? `/api/health/readiness`.
4. ???? ???????? ?? ??????????, ???????????? PostgreSQL backup.

### ????????? ???????????

- Remnashop ???????? ?????????? ?????? ??? ???????, ????????, ????? ? VPN-????????.
- Web DB ?????? ?????? ????????????? ??????: ??????, audit, payment records, integration status.
- Production Turnstile UI-widget ?????? ?????????? token ? BFF, ???? `TURNSTILE_ENABLED=true`.
- Sass `@import` warnings ???????? ?? ???????????? Sakai style ???? ? ?? ?????? ??????.

---

## English

Clean Pay is a standalone CleanVPN web cabinet built with Next.js App Router. The frontend talks only to the local BFF. The BFF talks to Remnashop Public API server-side, manages web-cookie sessions, cache DB records, audit logs, and Redis-backed rate limits.

### Architecture

- `web`: Next.js frontend + BFF.
- `postgres`: dedicated web-cabinet cache DB via Prisma.
- `redis`: rate-limit storage. Production has both bundled Redis and external Redis compose variants.
- `caddy`: reverse proxy on 80/443 with automatic TLS.
- `Remnashop`: external source of truth for plans, subscriptions, payments, and VPN access.
- `SMTP`: external SMTP dependency checked by readiness.
- `Cloudflare Turnstile`: optional server-side verification for sensitive auth actions.

### Devcontainer

Run project commands inside the existing devcontainer `clean-pay_devcontainer-app-1`. The devcontainer provides real networked dev dependencies:

- PostgreSQL: `db:5432`
- Redis: `redis:6379`
- Remnashop mock: `http://remnashop-mock:8080/api/v1/public`
- SMTP Mailpit: `mailpit:1025`, UI at `http://localhost:8025`
- Turnstile mock: `http://turnstile-mock:8787/turnstile/v0/siteverify`

```bash
cd /workspaces/clean-pay
npm install
npm run prisma:generate
npm run prisma:migrate -- --name init
npm run dev
```

### Commands

```bash
npm run lint
npm run build
npm run build:mock
npm run start:normal -- --hostname 0.0.0.0
npm run start:mock -- --hostname 0.0.0.0
npm run smoke:normal
npm run smoke:mock
```

### Environment

Use `.env.example` and `.env.production.example`. Never commit real secrets. Key groups: app URLs, PostgreSQL, Redis, Remnashop API, web secrets, cookies, SMTP, Telegram OIDC, Turnstile, and support contacts.

### Production With Bundled Redis

```bash
cp .env.production.example .env
# edit .env
docker compose --env-file .env -f docker-compose.prod.yml up -d --build
```

Services: `web`, `postgres`, `redis`, `caddy`. Caddy listens on `80/443` and manages TLS automatically.

### Production With External Redis

Set `REDIS_URL` and run:

```bash
docker compose --env-file .env -f docker-compose.prod.external-redis.yml up -d --build
```

Services: `web`, `postgres`, `caddy`.

### Cloudflare

Use Cloudflare SSL/TLS `Full (strict)`. Point `oplata.clear-vpn.org` to the server. Caddy manages origin TLS automatically. If Cloudflare proxy blocks HTTP/HTTPS challenges, configure Caddy DNS challenge separately.

### Health

- `GET /api/health`: public minimal health.
- `GET /api/health/readiness`: detailed DB, Redis, Remnashop `/plans/public`, and SMTP checks.

### Audit

User actions are stored in `AuditLog`; technical failures are emitted as JSON console logs. Secrets, cookies, JWTs, refresh tokens, verification codes, SMTP passwords, Turnstile tokens, and raw upstream details are not stored in audit metadata. IP addresses are stored only as hashes.

### Smoke And Acceptance

```bash
npm run smoke:mock
npm run smoke:normal
```

Manual real-Remnashop acceptance: registration, login, logout, e-mail confirmation, resend code, plan view, purchase, payment return, extension, current subscription, VPN link, device deletion, expired session, upstream errors, and mobile layout.

### Update

```bash
git pull
docker compose --env-file .env -f docker-compose.prod.yml up -d --build
docker compose --env-file .env -f docker-compose.prod.yml exec web npx prisma migrate deploy
```

### Rollback

Checkout the previous tag/commit, rebuild compose, verify health endpoints, and restore PostgreSQL backup if a DB migration cannot be rolled back safely.
