{
  "version": "2.0.0",
  "tasks": [
    {
      "label": "Clean Pay: install dependencies",
      "type": "shell",
      "command": "sh scripts/ensure-node-modules-owner.sh ${workspaceFolder}/node_modules node:node && npm install",
      "options": {
        "cwd": "${workspaceFolder}"
      },
      "problemMatcher": []
    }
  ]
}
